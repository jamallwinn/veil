{
  "name": "veil-circuits",
  "version": "1.0.0",
  "description": "ZK Circuits for Veil Privacy Pool",
  "private": true,
  "scripts": {
    "compile": "circom withdraw.circom --r1cs --wasm --sym -o ../build/",
    "info": "snarkjs r1cs info ../build/withdraw.r1cs",
    "print": "snarkjs r1cs print ../build/withdraw.r1cs ../build/withdraw.sym"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "snarkjs": "^0.7.4"
  }
}
